#include<stdio.h>
#include<conio.h>

/* unformated funtion ----> in this we have sertion input and output keyword. */
/* for input we use
    getch() , getche(), getchar(), gets().
*/
/* for output we use
    putch() , putche() , putchar()  , puts().
*/

void main() {
    int num;
    printf("enter any character or num : ");

    //code for getch() and putch()
    num= getch();
    putch(num);

    
    // code for getchar() and putchar() 
    num=getchar();
    putchar(num);

    //code for gets() and puts()
    
}